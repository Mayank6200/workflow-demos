name: First workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout repo
          uses: actions/checkout@v6 
          
        - name: Welcome message
          run: |
            echo " My second github workflow commaand"
            ls
            cat Index.html

        - name: Create snake
          run: cowsay -f snake " I am a python. "
